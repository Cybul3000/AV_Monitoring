# Implementation Plan: LG Pro Display — AV Monitoring Module

**Branch**: `002-lg-pro-display` | **Date**: 2026-03-25 | **Spec**: [spec.md](./spec.md)
**Input**: Feature specification from `/specs/002-lg-pro-display/spec.md`

## Summary

Implement an LG Pro Display monitoring and control module for the AV monitoring app. The module maintains a persistent TCP connection to each display on port 9761, sends the LG Monitor Control API command set (RS-232 / TCP variant), polls five state properties (power, input, screen mute, volume mute, volume level) on a configurable interval, and surfaces health via the standard LED status system. Control actions (power, input switch, mute, volume) are sent through the shared `device:command` IPC channel.

The LG Monitor Control API is an ASCII line-based protocol (`{cmd} {setId_hex} {data_hex}\r`) documented via the Bitfocus Companion module `bitfocus/companion-module-lgtv-prodisplay`.

## Technical Context

**Language/Version**: TypeScript 5.x (Electron 30 main process, Node.js 20 LTS)
**Primary Dependencies**: Node.js `net.Socket` (built-in) — no new npm packages
**Storage**: SQLite (`better-sqlite3`) — shared `devices` table; no dedicated migration needed
**Testing**: Vitest (unit + integration)
**Target Platform**: macOS 12+ and Windows 10+ (cross-platform, no native bindings)
**Project Type**: Electron desktop app module (same pattern as Zoom, Dante, Lightware modules)
**Performance Goals**: Poll response within one 5 s interval (SC-001/SC-002); TCP disconnection detected within 10 s (SC-003)
**Constraints**: Single command in-flight at a time (display limitation); volume clamped 0–100 (FR-011)
**Scale/Scope**: Up to 50 displays per network segment; Set ID range 0–99

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-checked after Phase 1 design.*

| Principle | Check | Notes |
|-----------|-------|-------|
| I. Module-First | ✅ PASS | All LG TCP communication in `src/main/modules/lg-display/`. Protocol sourced from `bitfocus/companion-module-lgtv-prodisplay` (cited in research.md). |
| II. Layered Hierarchy | ✅ PASS | LG devices exist within Rooms; LED aggregates upward. LGDisplayPanel rendered only within RoomView. |
| III. Verify, Never Assume | ✅ PASS | All command codes confirmed from Bitfocus source. No NEEDS CLARIFICATION items. |
| IV. Test-First | ✅ PASS | Unit tests in `tests/unit/lg-display/LGDisplayModule.test.ts`. |
| V. Device Registry | ✅ PASS | `lg-display` entry in `resources/device-registry.json`. `LGDisplayModule` registered in `src/main/modules/index.ts`. |
| VI. Configuration Integrity | ✅ PASS | No config download/restore for LG displays (no backup format in protocol). N/A. |
| VII. Observability | ✅ PASS | NG responses and connection events logged with `console.warn`/`console.log` with source prefix `[LGDisplayModule]`. |
| VIII. Cross-Platform | ✅ PASS | `net.Socket` is cross-platform. No platform-specific workarounds. |
| Protocol Confirmation (II.2a) | ✅ PASS | Protocol: TCP port 9761, ASCII LG Monitor Control API. Confirmed from Bitfocus Companion module source. |

**Post-design re-check**: All gates remain PASS after Phase 1 design.

## Project Structure

### Documentation (this feature)

```text
specs/002-lg-pro-display/
├── plan.md              # This file
├── research.md          # Phase 0 output — protocol details, command codes
├── data-model.md        # Phase 1 output — in-memory state, device registry entry
├── quickstart.md        # Phase 1 output — dev setup, file map, agent start files
├── contracts/
│   └── lg-ipc.md        # Phase 1 output — IPC channel contracts
└── tasks.md             # Phase 2 output (generated by /speckit.tasks)
```

### Source Code (repository root)

```text
src/main/modules/lg-display/
├── LGTCPTransport.ts         # Persistent TCP, command queue, exponential backoff reconnect
└── LGDisplayModule.ts        # DeviceModule — poll cycle, LED, 10 control commands

src/renderer/components/DeviceDetail/LGDisplayPanel/
└── LGDisplayPanel.tsx        # Power/input/mute/volume controls panel

tests/unit/lg-display/
└── LGDisplayModule.test.ts   # Unit tests for module logic and LED transitions
```

**Structure Decision**: Single project, extending the existing Electron app module pattern. Follows the same `src/main/modules/<type>/` and `src/renderer/components/DeviceDetail/<Type>Panel/` layout used by Zoom, Dante, Lightware, and Biamp modules.

## Complexity Tracking

No constitution violations. No complexity entries required.
